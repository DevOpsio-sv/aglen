/       /bg/    301
# The old #app / #ar home anchors are gone: the Unlocking Bulgaria promo is now
# the /ar-missions/ hub (M2, ADR-013). These vanity URLs point at it instead.
/play   /bg/ar-missions/   302
/ar     /bg/ar-missions/   302

# ── Legacy topic routes → the guide that replaced them ───────────────────
# These six routes rendered the same content as a /guides/ page, in all 14
# languages: 84 near-duplicate URLs competing with the pages that should rank.
# They now consolidate into the guide. The prerendered HTML is still generated
# (noindex, canonical → guide) as a fallback if a rule ever fails to match.
# `:lang` matches one path segment, so one rule covers every language.
/:lang/attractions/                     /:lang/guides/beautiful-places/    301
/:lang/attractions/vit-river/           /:lang/guides/vit-river/           301
/:lang/attractions/caves-rock-forms/    /:lang/guides/caves-and-rocks/     301
/:lang/travel-guide/food-local-products/ /:lang/guides/local-food/         301
/:lang/nearby-destinations/             /:lang/guides/nearby-destinations/ 301
/:lang/travel-guide/seasonal-guide/     /:lang/guides/seasonal-guide/      301

# ── M1: thin keyword/marketing landing pages → an existing page (301, all langs) ─
# Retired per IMPLEMENTATION_PLAN M1 (safe scope): every target returns 200 today
# (home, /location/, /local-businesses/, /about/). Entity/karst/plan targets are
# deferred until those routes exist (M3/M4). The six entity-content guide landings
# (Lukovit, Karlukovo, Krushuna, Devetashka, Iskar–Panega, Lovech) stay live.
# Prerendered HTML is still generated (noindex, canonical → target) as a fallback;
# these URLs are dropped from the sitemaps by isIndexableRoute.
/:lang/visit-aglen/                   /:lang/                    301
/:lang/things-to-do-in-aglen/         /:lang/                    301
/:lang/nature-around-aglen/           /:lang/                    301
/:lang/nature-tourism-aglen/          /:lang/                    301
/:lang/eco-tourism-bulgaria-aglen/    /:lang/                    301
/:lang/rural-tourism-bulgaria-aglen/  /:lang/                    301
/:lang/slow-travel-bulgaria-aglen/    /:lang/                    301
/:lang/cultural-tourism-aglen/        /:lang/                    301
/:lang/adventure-tourism-aglen/       /:lang/                    301
/:lang/hidden-places-near-aglen/      /:lang/location/           301
/:lang/aglen-route-map/               /:lang/location/           301
/:lang/traditional-food-aglen/        /:lang/local-businesses/   301
/:lang/accommodation-near-aglen/      /:lang/local-businesses/   301
/:lang/aglen-answer-hub/              /:lang/about/              301

# ── M2: the two legacy standalone Unlocking Bulgaria pages consolidate into the
# one local mission hub (ADR-013). /ar-missions/ returns 200 in every language;
# these pages render noindex with a canonical → /ar-missions/ as the fallback.
/:lang/unlockingbulgaria/            /:lang/ar-missions/        301
/:lang/app/                          /:lang/ar-missions/        301
